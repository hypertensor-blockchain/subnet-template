syntax = "proto3";

package api_protocol.pb;

message ApiProtocolMessage {
    enum ResponseType {
        UNARY = 0;
        STREAM = 1;
    }

    string route = 1;
    string method = 2;
    map<string, string> headers = 3;
    bytes body = 4;
    ResponseType response_type = 5;
}
